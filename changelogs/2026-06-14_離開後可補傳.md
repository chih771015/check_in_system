# 更動報告 — 2026-06-14（功能 1：離開後可補傳照片）

## Commits

| Hash | 說明 |
|------|------|
| `66a5d06` | feat: 離開打卡後允許翻譯員補傳照片（刪除/標未到仍鎖定） |
| `c861344` | docs: 同步「離開後可補傳」（刪除/標未到仍鎖） |
| （本檔） | docs: 更動報告 2026-06-14 |

## 變更摘要

### 做了什麼
情境：X 光 / 檢驗報告常在翻譯員離開後才出來，需要補傳。把上週的「離開後全鎖」**部分放開**：

- **離開後可 upload（補傳）**，但 **delete / no_show 仍鎖定**（只有管理員能刪/改狀態）。
- 後端：`UploadDiagnosis` 移除離開鎖定；`DeletePhoto` / `MarkNoShow` 保留鎖定。
- 前端：`DiagnosisUploadModal` 以 `canUpload` / `canDelete` 控制三模式（管理 / 補傳 append / 唯讀）；`MySchedules` 依排班狀態分三段（進行中完整編輯、離開後「補傳照片」append、未到達唯讀）。

### 影響範圍
- 後端：`service/diagnosis_service.go`（+測試）。
- 前端：`components/DiagnosisUploadModal`、`pages/translator/MySchedules`、`i18n`(3 語系，新增 `diagnosis.supplementPhotos`)。
- 文件：DIAGNOSIS_SERVICE_SPEC、PRODUCT_SPEC、USER_STORIES、PAGES_SPEC、COMPONENTS_SPEC、TEST-CASES、TEST-PLAN。

### 測試（TDD）
- 後端：離開後 upload 應成功；delete/no_show 仍 409。
- 前端：append 模式（可加不可刪）、唯讀模式。
- 全綠：後端 `go test ./...`；前端 `vitest` 83、`tsc`/`eslint` 乾淨。

### 注意事項
- 行為變更：`DIAGNOSIS_LOCKED_AFTER_LEAVE` 現在只在「離開後翻譯員刪除/標未到」時出現，不再擋上傳。
- 離開後 append 只對 completed / pending 顯示，不對 no_show（避免上傳把 no_show 翻成 completed）。
