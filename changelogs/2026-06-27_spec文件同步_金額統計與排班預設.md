# 更動報告 — Spec 文件全面同步（金額統計 / 排班預設 / typecheck）

- 日期：2026-06-27
- 分支：`feature/local-expose`
- 緣由：稽核所有 `*SPEC.md` 是否與現況相符，補上脫節處，並記下順手發現的問題。

## Commit

- `docs: spec 文件同步金額統計、排班預設視圖與 typecheck`

## 背景

以關鍵符號（StatsService / monthly-total / actualTotal / FindRecentByCreated / SumActual / statsEvents / formatNT …）grep 全部 spec，**沒有任何一個 spec 提到本批金額統計功能** → 整套「金額統計 + 排班預設視圖」當初未同步文件。本次補齊。

## 更新的 spec（14 檔）

### 後端
- `service/SERVICE_SPEC.md`：新增 **StatsService**（模組表 + §3 契約）；補 PatientService 的 `GetHistory(from,to)` 區間+驗證+批次照片、`ActualTotals`、`PatientYearActualTotal`；純函式清單加 `monthRange/nextDay/validDateOrEmpty`。
- `service/SCHEDULE_SERVICE_SPEC.md`：`List` 無篩選時回最近建立 100 筆（`created_at DESC`），有篩選回完整 `date ASC`。
- `repository/REPOSITORY_SPEC.md`：`FindRecentByCreated`、`SumActualByPatients/ByDateRange/ByPatientDateRange`（委派 `sumActualInRange`）、`FindBySchedulePatientIDs`、`UpdateActualAmount`。
- `handler/HANDLER_SPEC.md`：patient_handler 補 history 區間 / actual-total / 列表 actualTotal；新增 **stats_handler**。
- `cmd/server/SERVER_SPEC.md`：新增 `/admin/patients/:id/actual-total`、`/admin/stats/monthly-total` 路由。
- `dto/DTO_SPEC.md`：`PatientResponse.actualTotal`、`PatientHistoryResponse.actualTotal`。
- `model/MODEL_SPEC.md`：`schedules.date` 加 index。

### 前端
- `FRONTEND_SPEC.md`：結構圖加 `stores/statsEvents`、`utils/money`；§5 補兩者說明；§7 補新測試與 **typecheck 警語**。
- `api/API_CLIENT_SPEC.md`：`stats.ts`、`patients` 的 `getPatientActualTotal` / `getPatientHistory(range)`、`adminSetActualAmount` emit 事件。
- `components/COMPONENTS_SPEC.md`：AppLayout 橫幅（事件驅動刷新）、SchedulePatientListEditor 年度已實付提示、預付金額欄。
- `pages/PAGES_SPEC.md`：PatientManagement 實付總額欄、PatientHistory 區間+總額、ScheduleManagement 預設視圖/最新按鈕/依篩選刷新詳情；測試清單更新。

### 根層
- `PRODUCT_SPEC.md`：新增「金額統計與彙整」「排班預設視圖」兩段產品說明。
- `E2E_SPEC.md`：seed 表補今日排班 + patients[0] 實付 1500（`seededActualPaidTotal`）。
- `TESTING_SPEC.md`：見下「問題」。

## 順手發現的問題

1. **`TESTING_SPEC.md` 把 `npx tsc --noEmit` 寫成「型別檢查」**（§3 指令 + §5 CI 表）——正是先前修掉的 CI 空轉指令的文件版。已改為 `npm run typecheck`（`tsc -b`）並加警語。（CI workflow 本身已於 commit `cc5abd0` 修正。）
2. **根目錄同時存在 `SPEC.md`（原始產品規格書）與 `PRODUCT_SPEC.md`（維護中的 living spec）**：`SPEC.md` 的 DB schema（schedule_patients 無金額欄、schedules.date 無 index 等）與多次 migration 已脫節。本次**未**改寫 `SPEC.md`（屬歷史需求書，改寫整段 schema 風險/成本高）。**建議**：標註 `SPEC.md` 為原始版、以 `PRODUCT_SPEC.md` 為準，或安排另一次專門更新。待決。

## 驗證

- 全為文件變更（`.md`），不影響程式；未動任何 `.go`/`.ts`。
- 內容對照現行程式碼逐項確認（List 預設、repo 方法、handler 路由、dto 欄位、前端元件/頁面行為）。

## 影響檔案

14 個 `*SPEC.md`（如上）。`SPEC.md` 刻意未動（見問題 2）。
