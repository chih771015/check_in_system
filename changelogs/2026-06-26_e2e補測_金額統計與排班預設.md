# 更動報告 — e2e 補測：金額統計橫幅 + 排班預設/年度已實付

- 日期：2026-06-26
- 分支：`feature/local-expose`
- 動機：本分支 5 項需求多為前後端單元測試覆蓋，但 e2e（Playwright）尚未涵蓋。補上關鍵使用者流程。

## Commits

- `test(e2e): 補金額統計橫幅、排班預設按鈕與年度已實付的端對端測試`
- `docs: e2e spec 測試案例表 + 更動報告`

## 缺口盤點（5 項需求 × e2e）

| 需求 | e2e 前狀態 | 處理 |
|------|-----------|------|
| #1 後台當月總支出橫幅 | 無 | ✅ 新增 money-stats |
| #2 病人列表實付總額欄 + 歷史頁區間查詢 | 無（patient-history 僅看就診列表）| ✅ 新增 money-stats |
| #3 排班建立顯示病人年度已實付 | 無 | ✅ 補進 schedule-crud |
| #4 病人匯出實付總額欄 | 已被 backend 單元測試 + patient-import-export round-trip 間接涵蓋 | 不另補（見備註）|
| #5 排班預設 created_at + 最新創建排班按鈕 | 無 | ✅ 補進 schedule-crud |

## 新增 / 修改的測試

### `tests/money-stats.spec.ts`（新）
- admin 於後台頁可見「本月病人總支出」橫幅且帶 NT$ 數字。
- translator 登入後（等 app shell 與網路 idle、確認登出鈕可見）**不**顯示該橫幅 → 驗證 admin-only 角色閘門。
- 病人列表表頭含「實付總額」欄。
- 病人歷史頁顯示實付總額（Statistic）且有日期區間 RangePicker（`.ant-picker-range`）。

### `tests/schedule-crud.spec.ts`（擴充）
- 無篩選預設視圖顯示「最新創建排班」按鈕。
- 建立排班 modal：選翻譯員 + 填日期 + 選病人後，編輯器顯示該病人「{年} 年已實付」提示（會打 `/admin/patients/:id/actual-total`）。

斷言一律用三語 regex（zh-TW/en/th），與既有 spec 風格一致；所有 i18n 字串與選擇器已逐一對照前端原始碼確認存在。

## 驗證

- `npm install`（僅 test 依賴）後 `npx playwright test --list`：全部 spec 編譯通過、6 個新案例被正確發現、無 import/型別錯誤。
- **未**對 live docker stack 實跑（需 `npm run stack:up` 起 postgres+backend(e2e build)+frontend，較重）。要實跑：`cd e2e && ./run-e2e.sh`。

## 備註

- seed 的看診 `actual_amount` 為 0，故金額類斷言檢查「控制項/標籤/角色閘門有接上」而非特定金額；金額算術由 backend 單元測試覆蓋。
- #4 匯出第 5 欄「實付金額總額」：backend `BuildExcel` 單元測試已驗欄位、patient-import-export round-trip 已驗加欄不破壞再匯入；不重複在 e2e 解析 xlsx（e2e 無 xlsx parser 依賴）。
