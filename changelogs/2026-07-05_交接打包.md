# 更動報告 — 2026-07-05

## Commits

| Hash | 說明 |
|------|------|
| `c5fb4ce` | chore(handover): 新增交接打包腳本並忽略打包產出 |
| `fcb9fb4` | docs: 新增交接總入口文件 HANDOVER.md（中英雙語） |

## 變更摘要

### 做了什麼
- 決策：交接方式採「給原始碼 + 一鍵 `deploy-prod.sh` 自行 build」，不給預先做好的 image
  （因為接手者仍需自填密鑰、build 已被腳本一鍵化，image 無優勢反更麻煩）。
- 新增交接打包腳本 `scripts/package-handover.sh`：
  - 以 `git ls-files`（追蹤檔）+ `--others --exclude-standard`（未 commit 但未被忽略的新檔）
    匯出乾淨原始碼，自動排除 `node_modules`、`dist`、`uploads`、`.git`。
  - 強制剔除機密檔（`backend/.env`、`.env.production`、Google 憑證），並加一道保險：
    壓縮前掃描若仍有機密檔殘留即中止，避免外洩。
  - 產出 `dist-handover/翻譯員打卡系統_YYYY-MM-DD.tar.gz`（支援 `--zip`）。
  - 已實跑驗證：`HANDOVER.md` 有納入、`backend/.env` 確定被排除。
- 新增交接總入口文件 `HANDOVER.md`（中英雙語，僅中英兩種語言）：接手者第一份閱讀，
  三步部署並串接既有 `docs/PRODUCTION_DEPLOY.md`、`README.md`。
- `.gitignore` 忽略打包產出目錄 `dist-handover/`。

### 影響範圍
- 新增檔案：`scripts/package-handover.sh`、`HANDOVER.md`。
- 修改檔案：`.gitignore`。
- 不涉及 backend / frontend 程式邏輯，無需測試（屬工具與文件）。

### 注意事項
- ⚠️ `backend/.env`（含一組真 `JWT_SECRET`）目前仍被 git 追蹤。打包腳本已擋掉外洩，
  但建議根本解：`git rm --cached backend/.env`（檔案留在本機、不再進 git）。此項尚未執行，待決定。
- 交接時對接手者只需交代：解壓 → 讀 `HANDOVER.md` → 跑 `bash scripts/deploy-prod.sh`。
