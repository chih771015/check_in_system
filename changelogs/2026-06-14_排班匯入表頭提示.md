# 更動報告 — 2026-06-14（排班匯入加表頭提示，與病人匯入一致）

## Commits

| Hash | 說明 |
|------|------|
| `43661bc` | fix(frontend): 排班匯入按鈕也加「第一列為標題列」提示 |
| （本檔） | docs: 更動報告 2026-06-14 |

## 變更摘要

### 背景：檢查原本的排班匯入是否有同樣問題
- 排班匯入後端也是 `if i == 0 { continue }`（首列當表頭丟棄）→ **同樣**：無表頭的檔會掉第一列。
- 但排班匯入的緩解較完整：已有「下載範本」按鈕（範本含表頭 + 範例列）、且後端額外 `if len(r) == 0 { continue }`（空列略過，比病人版好）。
- 唯一不一致：少了病人匯入剛加的 Tooltip 格式提示。

### 做了什麼
- `ScheduleManagement` 匯入按鈕加 Tooltip（`schedules.importHint`，三語系），含「相同 Code 合併成一筆多病人排班」說明，與病人匯入一致。

### 影響範圍
- 前端：`pages/admin/ScheduleManagement.tsx`、`i18n`(3 語系)。純 UI 提示。

### 測試
- `tsc`/`eslint` 乾淨、i18n 三語系一致。

### 後續可選
- 病人匯入目前把「中間空列」回報為「缺少必填欄位」；排班匯入則靜默略過空列。若要完全一致，可讓病人匯入也略過全空列（低優先，未做）。
