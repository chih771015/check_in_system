# 更動報告 — 2026-05-07：階段 1 i18n（部分 — 後端 sentinel errors）

## 背景

雙 agent 並行執行 i18n + 病人資料庫時，i18n agent 受 sandbox 權限限制，只完成階段 1 的**後端 error code 化**，前端 react-i18next 接入、頁面翻譯、Header 切換器、axios interceptor 等仍待實作。

## 本次完成

### 後端
- `backend/internal/dto/error.go`（新）：
  - `ErrorResponse { Code, Message }` 結構
  - `NewError(code, message)` helper
  - 錯誤碼常量（如 `EMAIL_TAKEN`, `INVALID_CREDENTIALS`, `PATIENT_DUPLICATE` 等）
- `backend/internal/service/*.go`：5 個 service 改用 sentinel error
  - `admin_service.go`
  - `auth_service.go`
  - `checkin_service.go`
  - `schedule_service.go`
  - `translator_service.go`

### 編譯
- `go build ./...`：✅ 乾淨

## 待補項目（階段 1 剩餘）
1. Handler 層改寫：所有 `c.JSON(4xx, gin.H{"error":"..."})` → `dto.NewError(code, msg)`
2. 前端 react-i18next + i18next 安裝與初始化
3. 三語 JSON（en/zh-TW/th）
4. AppLayout Header 語言切換器
5. 所有頁面字串改 `t('...')`
6. axios interceptor 攔 `{code, message}` 對應 i18n key
7. `frontend/src/types/index.ts` 加 `ApiError`

後續以新 agent 或手動接續完成。
