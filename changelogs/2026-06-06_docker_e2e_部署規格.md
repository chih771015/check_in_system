# 更動報告 — 2026-06-06

## Commits

| Hash | 說明 |
|------|------|
| `b176306` | docs(spec): 新增 Docker 與 E2E 規格暨使用說明 |
| `b23594d` | docs(spec): 新增測試與部署規格，架構入口補連結 |

## 變更摘要

### 做了什麼
延續前一輪規格書產出，補齊 infra / 測試 / 部署四份文件（含使用說明與講解）：

1. **[docker/DOCKER_SPEC.md](../docker/DOCKER_SPEC.md)** — 三種 compose stack（dev / e2e / expose）、三個 Dockerfile、兩份 nginx 設定的差異與要點、port/volume 對照、啟停與升級指令、常見問題。
2. **[e2e/E2E_SPEC.md](../e2e/E2E_SPEC.md)** — Playwright 架構與資料隔離、`/api/test/reset` 三層保護、seed 資料（前後端單一真實來源）、`playwright.config` 設定解說、測試案例清單、三種跑法（run-e2e.sh / npm scripts / 純 docker）與失敗排查。
3. **[TESTING_SPEC.md](../TESTING_SPEC.md)** — 測試金字塔、後端（testify + in-memory SQLite）/ 前端（vitest）/ E2E（Playwright）各自怎麼測、怎麼跑、測什麼，CI workflow 兩個 job 解說，提交前自我檢查。
4. **[DEPLOYMENT_SPEC.md](../DEPLOYMENT_SPEC.md)** — 三種部署型態、環境變數清單、正式單機與 cloudflared 對外穿透步驟、資料持久化/備份、背景 cron、上線前安全檢查清單、疑難排解。

並在 [ARCHITECTURE_SPEC.md](../ARCHITECTURE_SPEC.md) 子規格索引補上「Infra / 測試 / 部署」區塊連結。

### 影響範圍
- **純文件新增 / 連結補充**，無程式碼 / 設定 / 行為變更。
- 新增 4 份規格、修改 1 份（ARCHITECTURE_SPEC 索引）。

### 注意事項
- 內容以實際檔案為準（docker-compose.*.yml、playwright.config.ts、run-e2e.sh、test.yml、scripts/expose.sh、.env.example、test_reset_handler.go）。
- 文件中重申幾項上線待強化項：CORS dev 全開需收斂、照片本機儲存無簽名 URL、E2E 尚未進 CI。
