# 更動報告 — 金額統計與排班預設排序（規格 / 計畫）

- 日期：2026-06-19
- 分支：`feature/local-expose`
- 類型：文件（spec + plan），尚未實作

## Commit

- `docs: 金額統計與排班預設排序 spec + plan`

## 做了什麼

針對需求方提出的五項新需求，先開規格與實作計畫文件（依 CLAUDE.md：開完不實作，等後續指令）：

1. 後台所有頁面最上方全域橫幅，顯示「當下月份」所有病人實付總額。
2. 病人實付查詢：列表顯示全時段實付總額 + 歷史頁日期區間篩選與加總。
3. 排班建立時，於病人列顯示該病人「排班所屬年份」已實付總額（唯讀）。
4. 病人 Excel 匯出新增「實付金額總額」欄（匯入/範本不變、保持相容）。
5. 排班頁預設 `created_at DESC` 取最近 100 筆 +「最新創建排班」高亮按鈕；套用其他篩選取消高亮，清空或按按鈕回到預設並高亮。

金額一律採用 `ActualAmount`（實付），`no_show`（actual=0）自然不計入。

## 影響檔案

- 新增 `docs/requirements/2026-06-19_金額統計與排班預設排序_spec.md`
- 新增 `docs/requirements/2026-06-19_金額統計與排班預設排序_plan.md`

## 需求方已確認決議

- 金額定義：全部用實付 `ActualAmount`。
- 後台首頁：不另開 dashboard，改全域橫幅、所有後台頁面可見。
- 月份範圍：只顯示當下日曆月份總額（依 `schedules.date`）。
- 病人時間軸查詢：列表總額 + 歷史頁區間查詢兩者都要。

## 待確認（實作前）

1. 需求3 年度已實付是否排除「編輯中未存檔」這筆（plan 預設：排除）。
2. 需求5 篩選模式是否也限制筆數（plan 預設：不限制，維持現況）。

## 備註

- 純文件，無程式碼變更，依 CLAUDE.md 不需 TDD。
- 後續實作時 plan 已規劃 staged commit 分層與每層 `go test ./...` / `npm test`。
