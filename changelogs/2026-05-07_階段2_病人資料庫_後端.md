# 更動報告 — 2026-05-07

## Commits

| Hash | 說明 |
|------|------|
| (此 commit) | feat: 病人資料庫後端 + 前端型別/API |

## 變更摘要

### 做了什麼

階段 2（病人資料庫）的後端骨架 + 前端型別/API client。Agent B 在背景執行時將檔案產出到 master（worktree 隔離未生效），因此一次性 commit 集中。

### 後端
- `model/patient.go`：Patient struct，含 `(id_type, id_number)` unique index
- `dto/patient.go`：CRUD request/response DTO、PatientHistoryResponse、PatientListResponse
- `repository/patient_repo.go`：含 `WithCtx(ctx)`，方法 Create / Update / Delete / FindByID / FindByIDTypeAndNumber / List(search, page)
- `service/patient_service.go`：CRUD + 重複檢查（IDNumber 入庫前 ToUpper）、`GetHistory` 階段 2 回空陣列（TODO 標 Stage 4）
- `handler/patient_handler.go`：admin CRUD + translator GET
- `cmd/server/main.go`：AutoMigrate 加 Patient、初始化 PatientService/Handler、註冊 admin + translator 路由

### 前端
- `types/index.ts`：新增 `IDType`、`Patient`、`TranslatorPatient`、`PatientHistoryEntry`、`PatientListResponse`、`PatientHistoryResponse`
- `api/patients.ts`：getPatients / createPatient / updatePatient / deletePatient / getPatientHistory / getPatientsForTranslator

### 影響範圍

| 範圍 | 檔案 |
|------|------|
| 後端 model | `backend/internal/model/patient.go`（新增）|
| 後端 DTO | `backend/internal/dto/patient.go`（新增）|
| 後端 repo | `backend/internal/repository/patient_repo.go`（新增）|
| 後端 service | `backend/internal/service/patient_service.go`（新增）|
| 後端 handler | `backend/internal/handler/patient_handler.go`（新增）|
| 後端路由 | `backend/cmd/server/main.go`（修改）|
| 前端 types | `frontend/src/types/index.ts`（修改）|
| 前端 API | `frontend/src/api/patients.ts`（新增）|

### 編譯驗證
- `go build ./...`：✅ 乾淨
- `npx tsc --noEmit`：✅ 乾淨

### 後續
- 前端 admin 頁面 / history placeholder / menu 項目尚未產出，agent 仍在執行
- i18n agent 也在跑（階段 1），完成後合併
