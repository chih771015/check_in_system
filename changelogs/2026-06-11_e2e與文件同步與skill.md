# 更動報告 — 2026-06-11（e2e 測試 + 文件同步 + sync-docs skill）

## Commits

| Hash | 說明 |
|------|------|
| `554dd33` | test(e2e): 新增診斷照片管理與重複 email 的端對端測試 |
| `8809c8e` | docs: 同步三項功能異動到規格/使用者故事/測試文件 |
| `a24fe8f` | chore: 新增 /sync-docs skill — 功能異動的文件同步清單 |
| （本檔） | docs: 更動報告 2026-06-11 e2e 與文件同步 |

## 變更摘要

### 做了什麼
延續同日的三項功能修改（診斷照片刪除+補傳、重複 email 錯誤回饋、照片永久保存），補上 e2e 測試、全面同步文件，並建立一個可重用的 skill。

**1. e2e 測試**
- `e2e/tests/diagnosis-manage.spec.ts`（新增）：API 層驗證上傳→補傳→刪除→刪光退回 pending，以及刪除不存在照片回 404。走 Playwright `request` 避開「管理按鈕需先 arrived 打卡」的 geolocation 障礙。
- `e2e/tests/translator-mgmt.spec.ts`：新增重複 email → 顯示 `EMAIL_TAKEN` 訊息。

**2. 文件同步（一次找齊並修正）**
- 後端 spec：DIAGNOSIS_SERVICE / HANDLER / SERVER / REPOSITORY / DTO / CONFIG / SERVICE。
- 前端 spec：COMPONENTS / PAGES / API_CLIENT。
- 產品 / 使用者 / 測試：PRODUCT_SPEC、USER_STORIES（加 Epic 7 + 驗收 + 場景 F）、TEST-CASES、TEST-PLAN、E2E_SPEC。
- **修正過期內容**（非僅新增）：TEST-CASES TC-TM-004、TEST-PLAN 2.2.6 的「重複 email」原寫 400/"email already exists"，更正為 **409 `EMAIL_TAKEN`**；照片保留相關預設由 90 → **0（永久保存）**。

**3. `/sync-docs` skill**
- `.claude/commands/sync-docs.md`：把「功能異動要改哪些文件」固定化。
  - **固定文件**（每次必看）：changelog、PRODUCT_SPEC、USER_STORIES、TEST-CASES、TEST-PLAN、e2e/E2E_SPEC。
  - **浮動文件**（依改動相對對應）：code 目錄 → 同層 `*_SPEC.md` 對照表。
  - **跨切面觸發規則**：error code / API 端點 / 環境變數 / cron / 狀態機 / repository / i18n key 各自的連帶必改清單。
  - 附自我稽核 `grep -rl` 流程與收尾檢查清單。

### 影響範圍
- e2e：`e2e/tests/`、`e2e/E2E_SPEC.md`。
- 文件：根目錄 4 份大文件 + 後端/前端各層 `*_SPEC.md`。
- 工具：`.claude/commands/sync-docs.md`（專案層 slash command，團隊共用）。
- **無 code（go/ts）邏輯變更**，故不需新增單元測試。

### 注意事項
- e2e 新測試已通過 `tsc` typecheck；完整執行需 docker e2e stack（`-tags e2e` + `ENABLE_TEST_RESET=true`），見 `e2e/HOW_TO_RUN.md`。
- `/sync-docs` 之後可直接呼叫：每次做完功能就跑一遍，依清單補文件。
- 文件統計數字為近似值（標示 ~），重點在對齊行為與修正過期描述。
