# 更動報告 — 2026-06-11（診斷結果總覽可直接管理照片與狀態）

## Commits

| Hash | 說明 |
|------|------|
| `832d66d` | feat(frontend): 診斷結果總覽可直接管理照片與狀態 |
| `0d9b5f9` | docs: 同步診斷結果總覽可直接管理照片與狀態 |
| （本檔） | docs: 更動報告 2026-06-11 |

## 變更摘要

### 做了什麼
讓管理員不必繞到「排班管理」，直接在 `/admin/diagnosis-results` 編輯診斷結果：
- 每列新增「操作」欄：
  - **管理照片**：開 admin 版 `DiagnosisUploadModal`（增刪/補傳），注入 `adminListDiagnosisPhotos`/`adminDeleteDiagnosisPhoto`/`adminUploadDiagnosis`，完成後 refetch。
  - **completed 列**另有「標記未到」：開 admin 版 `NoShowModal`（`adminMarkNoShow`）。
  - **no_show 列**只給「管理照片」：補傳一張照片即把狀態翻回 completed。
- 管理員不受離開鎖定限制，任何 terminal 列皆可改。
- 純前端：沿用既有 modal 與 admin API，**不需後端改動**。操作欄 `fixed:right`、表格 `scroll.x` 調寬。

### 影響範圍
- 前端：`pages/admin/DiagnosisResults.tsx`（+ 新測試）。
- 文件：PAGES_SPEC、PRODUCT_SPEC、TEST-PLAN。

### 測試
- 新增 `DiagnosisResults.test.tsx`（stub 子 modal）：completed 列可開「管理照片」「標記未到」、no_show 列只可「管理照片」，且 modal 帶正確 `schedulePatientId`。
- 前端 `vitest` 82 全過、`tsc`/`eslint` 乾淨。

### 注意事項
- 「標記未到」會清空該列照片（沿用 no_show 清照片行為）；總覽頁的「管理照片」與「查看照片」並存（後者為快速唯讀預覽）。
